# Generated results

Your pipeline creates these files; do not fill them in manually:

```text
results/
  part1/
    data_issues.parquet
    run.json
    row_counts.json
    source_trace.parquet
    trace_examples.json
    analysis/
  part2/
    predictions.parquet
    metrics.json
    run.json
    report.md
```

The exact fields and tracing rules are in the assignment brief. Keep large or
temporary generated files out of version control, but retain this README so
the required layout is visible in a clean starter workspace.
